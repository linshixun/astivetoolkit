<project basedir=".." default="generate-version">
    <target name="generate-version">
        <mkdir dir="${src.dir}/main/org/astivetoolkit"/>
        <echo file="${src.dir}/main/org/astivetoolkit/Version.java">
            package org.astivetoolkit;

            /** Automatically generated by ant. */
            public class Version {
            public static final String VERSION = "${version}";
            public static final String BUILD_TIME = "${build}";

            // Used by version.sh script.
            static public void main(String... args) {
            System.out.println(VERSION);
            }
            }
        </echo>
    </target>
</project>