mvn install:install-file \
    -Dfile=astive-archetype-${project.version}.jar \
    -DgroupId=com.phonytive.astive \
    -DartifactId=astive-archetype \
    -Dversion=${project.version} \
    -Dpackaging=jar
